/* -----------------------
   TOOLS
----------------------- */

export type ToolsSection = {
  openingHours: boolean;
  banner: boolean;
  enquiries: boolean;
  services: boolean;
  updates: boolean;
};